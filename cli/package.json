{
  "name": "@carbonveritas/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "cv": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "lint": "eslint src --ext .ts",
    "test": "jest"
  }
}
